<?php

// Page Meta
set( 'page.title', '' );
set( 'page.description', '' );

get_header(); ?>

    <div class="container">
        <h1>404.</h1>
        <p>We couldn't find that page, sorry about that.</p>
    </div>

<?php get_footer(); ?>
